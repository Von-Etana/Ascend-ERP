---
deployment:
  tasks:
    - export DEPLOYPATH=/home/$USER/ascend_app
    - export PUBLICPATH=/home/$USER/public_html
    - /bin/mkdir -p $DEPLOYPATH
    - /bin/cp -R * $DEPLOYPATH/
    - /bin/cp -R public/* $PUBLICPATH/
